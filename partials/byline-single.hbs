{{#primary_author}}

    <section class="media">

        {{#if profile_image}}
            <div>
                <a href="{{url}}" class="avatar avatar--link">
                    <img
                        src="{{img_url profile_image size="avatar_medium_2x"}}"
                        loading="lazy"
                        class="avatar__image"
                        alt="{{name}}"
                    />
                </a>
            </div>
        {{/if}}

        <div class="author-card">

            <h3 class="author-card__name">
                <a href="{{url}}" class="link-unstyled">{{name}}</a>
            </h3>

            <p class="author-card__bio">
                {{#if bio}}
                    <span class="author-card__text js-nbsp">
                        {{bio}}
                    </span>
                {{/if}}
                <span class="author-card__links">
                    <a href="{{url}}" class="author-card__link">
                        Sobre o autor
                    </a>
                </span>
            </p>

        </div>
    </section>

{{/primary_author}}
