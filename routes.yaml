routes:
  /registrace/: members/signup
  /prihlaseni/: members/signin
  /ucet/: members/account

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: primary_tag:clanek
    data: tag.clanek

taxonomies:
  tag: /tema/{slug}/
  author: /autor/{slug}/
