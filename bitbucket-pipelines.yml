image: node:8

pipelines:
    default:
        - step:
            caches:
                - node
            script:
                - npm install --global gscan
                - npm install
                - npm run build
                - npm test
